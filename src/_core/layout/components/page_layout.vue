<template>
  <div>
    <slot />
  </div>
</template>

<script setup lang="ts">
/**
 * Thin wrapper for feature pages inside the `app` layout.
 * Set `definePageMeta({ layout: 'app', navTab, appTitle })` on the route instead.
 */
</script>
